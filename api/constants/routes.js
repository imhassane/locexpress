module.exports = {
    accounts: {
        root: '/accounts',
        AUTHENTICATE: 'authenticate'
    },

    properties: {
        root: '/properties',
        GET_ALL: '/',
        CREATE: '/'
    }
}